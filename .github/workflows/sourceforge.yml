name: sourceforge..

on:
#  release:
#    types: [published]
#  push:
#    branches:
#      - master
#    paths:
#      - '.config'
#  schedule:
#    - cron: 0 8 * * 5
  watch:
    types: [started]
    
env:
  ROM_URL: http://tdrive.manofuranium.workers.dev/www.stockrom.net_A920FXXU1BSC5_A920FOXM1BSC5_ZTO.zip
  ROM_NAME: OneUI
  ZIP_NAME: OneUI
  DATE_TODAY: 20200712
  ANDROID_VERSION: 09
  TZ: Asia/Kolkata

jobs:
  build:
    runs-on: ubuntu-latest


    steps:
       - name: Checkout
         uses: actions/checkout@master
           
       - name: Initializing environment
         run: |
            sudo apt update 2>/dev/null | grep packages
            sudo apt install wget
            sudo apt-get install p7zip-full

       - name: Downloading the File
         run: |
            mkdir file
            sudo chmod 0777 file
            cd file
            sudo wget -O file.exe http://tdrive.manofuranium.workers.dev/VirtualBox-6.1.10-138449-Win.exe
            ssh -o StrictHostKeyChecking=no ruiii@frs.sourceforge.net uptime
            chmod 600 ~/.ssh/authorized_keys
            sftp ruiii@frs.sourceforge.net 
            princerouel123
            cd /home/frs/project/rui-ports/test
            put file.exe
